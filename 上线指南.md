# AI 洞察知识库 · 上线与自动化指南

## 这个文件夹里有什么

```
ai-kb/
├── index.html            ← 知识库首页（日历索引 + 全文搜索，双击即可本地打开）
├── build_index.py        ← 首页重建脚本（每次新增日报后跑一次）
├── manifest.json         ← 日报清单
├── search-index.json     ← 搜索索引（344 条情报全文可搜）
├── digests.md            ← 13 天结构化摘要（洞察分析的原料）
├── reports/              ← 13 份每日日报原文（2026-07-25 ~ 08-07）
├── insights/
│   └── 2026-08-第一期.html  ← 观点报告第一期（6 个判断，全部数据可回溯）
└── deck/
    └── AI高层谈资-2026-08.html ← 深色高端横滑 PPT（14 页，浏览器打开，←→ 翻页）
```

## 第一步：发布到 GitHub Pages（约 10 分钟，一次性）

1. 登录 github.com，右上角 + → New repository
   - 名称建议：`ai-insights`，选 **Public**（Private 也能开 Pages，但需付费版；Public 仓库无所谓，内容本来就是发出去的日报）
2. 把 `ai-kb` 文件夹里的全部内容上传到仓库（网页端 Add file → Upload files，直接拖进去）
3. 仓库 Settings → Pages → Source 选 `Deploy from a branch`，Branch 选 `main` / `(root)`，保存
4. 约 1 分钟后，知识库上线：
   - 首页：`https://<你的用户名>.github.io/ai-insights/`
   - 任意一天：`https://<你的用户名>.github.io/ai-insights/reports/2026-08-07.html`
   - 观点报告：`https://<你的用户名>.github.io/ai-insights/insights/2026-08-第一期.html`

## 第二步：让每日日报自动归档（需要一个 Token）

1. GitHub 头像 → Settings → Developer settings → Personal access tokens → **Fine-grained tokens** → Generate new token
   - Repository access：只选 `ai-insights` 这一个仓库
   - Permissions：Contents → **Read and write**，其他都不给
   - 有效期建议 1 年
2. 把 token 交给 Claude（对话里说"配置日报归档，token 是 xxx"即可），Claude 会把日报定时任务改造成：
   - 生成并发送日报（现有流程不变）
   - 追加一步：把当天 HTML 提交到仓库 `reports/`，更新 manifest 与搜索索引，重跑 `build_index.py`，push
   - 归档失败不影响发报，只在任务结果里提示

> Token 只授权这一个仓库的内容读写，泄露也动不了别的东西；到期换新即可。

## 已经生效的自动化（无需 GitHub，今天就在跑）

| 任务 | 时间 | 内容 |
|---|---|---|
| ai-weekly-signals | 每周五 17:00 | 回读本周日报 → 提炼 ≤5 条"本周信号"+"本周一个数字" → 发到你的两个邮箱 |
| ai-monthly-insights | 每月 1 日 09:00 | 回读上月全部日报 → 观点报告（6 主线追踪 + 与上月对比）+ 谈资 Top 10 → 发到你的两个邮箱 |

注意：定时任务在 Claude 桌面端打开时运行；错过会在下次启动时补跑。建议对两个任务各手动 Run now 一次，把权限预先批掉，以后就不会卡在授权上。

## 日常使用

- **查资料**：打开知识库首页，搜关键词（如"CoreWeave"、"开源"、"电力"），命中条目直达当天日报
- **高层交流**：打开 `deck/AI高层谈资-2026-08.html` 全屏放映，或发链接（上线 GitHub Pages 后）
- **月度更新 PPT**：每月观点报告出来后，对 Claude 说"按新一期观点更新谈资 PPT"即可
